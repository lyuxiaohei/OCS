# Grilling 进度快照 — `PRD-后台-V2.0.md`

> `/grill-with-docs` 会话进度。最后更新 2026-06-18。
> **续接方式**：新会话读本文件 + `CONTEXT.md` + `docs/adr/0001~0007` 即可无缝继续。

## 会话约定

- 用户是**产品经理**，只拷问**产品架构 + 实现逻辑**（状态机/业务规则/角色边界/单据流转/边界场景），**不拷问代码实现**（表/接口/grep 后端）—— 见 memory `user-role-pm-grill-scope`。
- 节奏：每题附推荐答案、一次一问，用户用 A/B/C 选。
- 产物均在 `prototype-cs/doc-pm/V1.0/`：`CONTEXT.md`（术语表）+ `docs/adr/`（决策）。

## 一、已决（详见各 ADR）

| ADR | 决策 |
|---|---|
| 0001 | 角色拆 4 个：`C / A / S / SYSTEM`（S=客服主管·人 source=1；SYSTEM=后端自动 source=2），废弃 SYS 一码双义 |
| 0002 | 操作归属：A=单会话执行 / S=跨会话全局管理监督 / SYSTEM=非人自动；5 争议格裁定（仪表盘 A+S、待办催办/规则=S、差评追踪=S发起+A补救、转接回退配置=S、负载面板 A+S） |
| 0003 | 转接生命周期：pending 源只读·转接中态 / accepted 写权全转·源只读历史 / 连环限深 3 跳 / 离线宽限 2min 超时重分 |
| 0004 | 留言闭环：invalid 对外静默 / 回复回灌下次会话（不主动推送） |
| 0005 | 评价闭环：催评限 2 次 / 统计按最终态 / 拆 skipped(主动)+expired(超时) 差异化催评 |
| 0006 | 消息审核可见性：撤回·隐藏双向 / 脱敏单向（用户看原文）/ 原文全留痕 |
| 0007 | 敏感词词库由商城统一供给，OCS 不自建（外部集成）；删设置敏感词库区块；策略+开关归 OCS 租户（默认开/替换\*\*\*） |
| 0008 | 敏感词商城不可达 → fail-open 全放行，不自留红线最小集（纯消费方，合规取舍显式承担） |
| 0009 | 挂起恢复归属：默认回原客服，恢复瞬间原客服离线即重分（trigger 无关）；挂起期间用户消息不丢 |
| 0010 | 客户信息一期范围：服务+基础+交易（行为/埋点推 2 期、灰显占位）；一期外部集成=商城会员+OMS 两源 |

### 二轮审阅补决（2026-06-22，Q1-Q7 边角，全部按推荐方案）

| Q | 决策 | 落 ADR |
|---|---|---|
| Q1 | 转接重新排队累计 **2 次**终止兜底（优先回原客服 / 否则关闭+致歉+留言邀约） | 0003 |
| Q2 | 催评口径统一：**会话封顶 2 次**，主动跳过（skipped）**提前停**（消解"限2次 vs skipped1/expired2"矛盾） | 0005 |
| Q4 | 转接失败回流时，队列中的用户消息**退原客服、不丢** | 0003 |
| Q5 | 差评追踪**一期=记录跟进+主管手动补偿**，不挂客服绩效（绩效 2 期） | 0002 |
| Q6 | 评价 expired 阈值 = **会话关闭后 48h** | 0005 |
| Q7 | 挂起期间客服离线 = **挂起优先**（不套离线宽限，按恢复时间走；恢复到期仍离线才重分） | 0009 |

> 二轮撤回（查证不严）：Q3（转接回退策略原型 vs PRD——实测原型 select 有 3 项、与 PRD 一致，无问题）、Q8（仪表盘权限——ADR-0002 已决 A/S 都可查看）。

## 二、未决 / 待拷问（按重要性）

1. ~~**富媒体卡片跨端兼容**~~ — **dev 层（消息协议/渲染降级机制），PM 不决，跳过**；产品侧只保留「消息必达」原则，具体双发/探测/兜底交工程。
> **本节产品层拷问 100% 完成**（ADR 0001-0010 + Q14b / Q18 / Q19 / Q20 / Q21）。所有产品架构与逻辑缝隙已收口，剩余仅 dev 层（见下）。
> dev 层跳过（PM 不决）：富媒体卡片跨端兜底、知识库并发编辑锁机制 + 审核闭环字段（审核 5 态状态机 PRD 已完整，无产品缺口）。
> 敏感词链已全闭（0007+0008+Q14b）。挂起恢复归属已决（ADR-0009）。客户信息一期范围已决=服务+基础+交易、行为推 2 期（ADR-0010）。待办关闭=定格 skipped+二次确认（ADR-0005 续）。

## 三、PRD 修订清单（grilling 收尾后执行 — 大活，需新会话）

把上述决策回写进 `PRD-后台-V2.0.md`：

- **角色列重排**：全文 UC + 状态机角色列改 `C/A/S/SYSTEM`（含差评追踪 S发起+A补救、待办催办/规则=S、转接回退配置=S、仪表盘=A/S）
- **§二角色表**：4 角色，去掉 SYS 双义描述
- **状态机**：会话补「转接中」条件（accept+pending 转接单）；评价拆 `skipped`+`expired`（4 态）
- **§设置**：删「敏感词库」区块（4 分组/20 词/导入导出/新建表 `customer_chat_sensitive_words`）；保留（待定）命中策略/开关
- **§1.1 + §五**：加「实现来源」列，两维度四值（复用/新建表/聚合/外部集成）；知识库、留言标新建表，敏感词/客户信息(基础/行为/交易)标外部集成+「待确认外部接口」
- **§5.2 #6**：留言通知改"回复回灌下次会话、不主动推送"；系统设置「留言通知方式」下拉语义调整
- **消息审核**：补用户侧可见性（撤回/隐藏双向、脱敏单向、留痕）
- **转接**：pending 源只读/交接写权全转/连环限深 3/离线宽限 2min 重分/失败回流（rejected·cancelled 回源、timeout 走 3 选 1）
- **历史会话客服列**：由单名/双名扩展为全链 `A→B→C`
- **§知识库.3 / §4.3**：修正"引用了不存在的表"措辞（knowledge_base/kb_categories/留言表显式标新建表+建议表名）
- 保留既有差异标注（评价单一星值非四维、话术业务 4 类≠快捷回复 6 类、系统设置无 pending_resume_default 等）
- **新增（Q16-Q19 决）**：① 仪表盘今日概况 6→5 项（删「AI 托管」位，3 期加回）；② 待办「待评价-关闭」= 定格 skipped 终态 + 二次确认（§待办.2.3 / UC-待办-07 改，衔接 ADR-0005）；③ 挂起恢复归属 = 回原客服、恢复瞬间原客服离线即重分、挂起期间用户消息不丢（§工作台.2.1 补，ADR-0009）；④ 客户信息行为 Tab 灰显占位「即将上线」、一期仅 3 有效 Tab（§客户信息.2.1 改，ADR-0010）
- **话术变量兜底（Q20 决）**：变量（尤其 `{订单号}`）填充失败 → **阻止发送 + 标红提示客服**（区分「用户暂无订单」/「数据获取失败」），客服手动修改后发（§话术.2.2 补）；否决照发留字面（暴露模板）/ 智能省略（语义错乱）
- **转接连环深度边界（Q21 决，补 ADR-0003）**：深度计数只累加客服主动转接；timeout 回退走「重新排队分配」→ **计数重置归零**（重分后新客服有完整 3 跳额度）；避免会话被永久卡限深边缘（§转接.2 / §4.2 补一句）

## 四、待用户拍板的产品范围决策（非逻辑、属取舍）

- ~~客户信息一期范围~~ ✅ 已决=服务+基础+交易、行为推 2 期（ADR-0010，候选 C）
- ~~敏感词命中策略与全局开关归属~~ ✅ 已决=归 OCS 租户（Q14b / ADR-0007）

> §四 两项范围决策均已拍板。

# 排期表格冲突分析报告

> 基于 `线上客服排期_已分组.xlsx` 5 个分表交叉比对
> 生成时间：2026-06-10

---

## 总览

5 个分表整体框架一致（负责人分配、功能数量、依赖层级均对得上），但发现 **7 个冲突点**：

| # | 严重度 | 问题 |
|---|--------|------|
| 1 | 🔴 高 | 留言离线管理排早了，跟依赖功能撞车 |
| 2 | 🟡 中 | 知识库说被聊天窗依赖，聊天窗没认 |
| 3 | 🟡 中 | 第1周4个任务并行，甘特图看不出分阶段 |
| 4 | 🟡 中 | 消息已读/未读已有代码，却归2期 |
| 5 | 🟡 中 | 排队等待提示代码已有，表中标"无" |
| 6 | 🟡 中 | 订单/商品卡片等3个功能1期2期边界不清 |
| 7 | 🟠 低 | 智能路由三个表描述各不相同 |

---

## 🔴 冲突 1：留言离线管理排早了（最严重）

### 什么情况

B 同学负责的「留言离线管理」（B7）必须等「人工客服交互」（C4）做完才能开始——因为要先把用户填什么字段定下来，才能去做管理页面。

但排期把这两个都放在了**第 2 周**，等于前一个还没做完，后一个就开始了。

### 对比

同样是 L3 层、同样依赖 C4 的「评价管理」（B6），排期就放到了**第 3 周**，这才是合理的。

### 建议

**把 B7 留言离线管理从 W2 挪到 W3。**

如果 B 同学 W2 有空档，可以先把 UI 线框画出来，逻辑对接放到 W3。

---

## 🟡 冲突 2：知识库说自己被依赖，聊天窗不认

### 什么情况

- 「知识库管理」（B5）的"被谁依赖"写了 **"C3, B2"**（意思是聊天窗会用到我）
- 但「聊天窗」（B2）的依赖清单里，强依赖和弱依赖都没提 B5

### 问题

两边说法不统一。到底聊天窗用不用知识库的数据（比如快捷回复）？

### 建议

- 如果用 → 在 B2 聊天窗的弱依赖里补上 B5
- 如果不用 → 把 B5 的"被谁依赖"里的 B2 删掉

---

## 🟡 冲突 3：第1周排了太多有依赖关系的任务

### 什么情况

第 1 周同时安排了 4 个任务开工：

| 层级 | 任务 | 负责人 |
|------|------|--------|
| L0 | 会话配置中心（定义路由规则） | B |
| L0 | 知识库管理（定FAQ分类） | B |
| L1 | 聊天窗 UI | A |
| L1 | C端聊天窗 UI | B |

后面两个（L1）要等前面两个（L0）出结果才能接逻辑。

### 问题

依赖分层表确实标注了"先画UI、逻辑后等"，但**甘特图上看不出这种分阶段**，容易让人以为 4 个都能在第 1 周全部完成。

### 建议

在甘特图的 W1 格子里标清楚：
- 聊天窗（A）：▶ W1 画UI，逻辑等B3出结果
- C端聊天窗（B）：▶ W1 画UI，逻辑等B3出结果

---

## 🟡 冲突 4：消息已读/未读——已有代码却归2期

### 什么情况

代码里已经实现了消息已读/未读（`ActionRead` + `is_read`），但排期表把它标为"2期-体验提升"，本期不做。

| 字段 | 值 |
|------|-----|
| 是否有此功能 | 有 |
| 开发分期 | 2期-体验提升 |
| 本期(1期-MVP) | 否 |

### 问题

功能已有却说"2期才做"，开发看了会困惑——到底还做不做？

### 建议

改成以下两种之一：
- **"已有，无需开发"** → 从排期表中去掉
- **"已有基础版，2期增强"** → 在备注里写清楚 2 期具体增强什么（比如批量已读、已读回执推送等）

---

## 🟡 冲突 5：排队等待提示——代码已有但标"无"

### 什么情况

代码里已有 `waiting-user-count` action（能推送排队人数），但排期表标"是否有此功能 = 无"，归到 2 期。

### 问题

会漏掉对现有代码的复用，开发可能从零开始做。

### 建议

把"是否有此功能"改成**"有（部分）"**，备注写"已有排队人数推送，2期需补充预计等待时间、留言选项"。

---

## 🟡 冲突 6：订单卡片/商品卡片/富媒体——1期2期边界不清

### 什么情况

这三个功能的开发分期都写的是**"1期-MVP/2期-体验提升"**，本期标注"是"，但没说清楚 1 期做到什么程度、2 期做到什么程度。

| 功能 | 分期标注 | 本期 |
|------|---------|------|
| 订单卡片交互 | 1期/2期 | 是 |
| 商品卡片交互 | 1期/2期 | 是 |
| 富媒体消息 | 1期/2期 | 是 |

### 问题

在线客服排期的备注提到订单卡片"可以选择订单进行操作（退换货、取消订单、修改地址）"，但这些操作算 1 期还是 2 期？没人说清楚。

### 建议

明确拆分：

| 功能 | 1期-MVP | 2期-体验提升 |
|------|---------|-------------|
| 订单卡片 | 展示订单信息卡片 | 卡片内操作按钮（退换货/取消/改地址） |
| 商品卡片 | 展示商品信息卡片 | 卡片内加购按钮 |
| 富媒体消息 | 图文消息 | 视频消息、优惠券卡片 |

---

## 🟠 冲突 7：智能路由三个表描述各不相同

### 什么情况

同一个功能「智能路由与分流」，三个表的备注写的不一样：

| 来源 | 描述 |
|------|------|
| 在线客服排期 | "标签（客户页，标记未什么入口进入）" |
| 原型分组视图 | "★核心:规则编辑器+路由引擎" |
| 分工方案 | "★路由规则编辑器+敏感词库+自动化配置" |

### 问题

在线客服排期的备注只提了一个很小的子功能（入口标签），完全没提到核心的"规则编辑器+路由引擎"，会让人低估工作量。

### 建议

统一为：**"★核心：规则编辑器+路由引擎+入口标签"**

---

## 修复优先级建议

| 优先级 | 冲突 | 修复动作 |
|--------|------|---------|
| **立即改** | 冲突1 | B7 从 W2 挪到 W3 |
| **尽快改** | 冲突6 | 拆清3个卡片功能的1/2期边界 |
| **尽快改** | 冲突7 | 统一智能路由描述 |
| **有空改** | 冲突2 | 确认B5和B2的依赖关系 |
| **有空改** | 冲突3 | 甘特图加"UI/逻辑"分阶段标注 |
| **有空改** | 冲突4/5 | 已有功能的状态标注修正 |
